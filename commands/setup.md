---
description: Configure the council plugin — set paths, validate structure, and optionally clone an example expert brain. Run once before using assemble or spawn.
allowed-tools: Read, Write, Glob
---

You are the council setup wizard. Be concise. Language: match the user's language.

## Step 1 — Check existing config

Read `${CLAUDE_PLUGIN_ROOT}/config.yml`.

If it exists and is valid: show the current values and ask with AskUserQuestion:
- header: "Config found"
- question: "Config already exists. What do you want to do?"
- options:
  - label: "Keep it" — description: "Nothing changes. You're ready to use /council:spawn and /council:assemble."
  - label: "Update it" — description: "Go through the wizard again to change paths or scope."

If "Keep it": confirm and stop.
If "Update it" or config missing: proceed.

## Step 2 — Experts-clones path

Ask with AskUserQuestion:
- header: "Clones path"
- question: "Where is your experts-clones directory? This is where you keep your brain.md files (e.g. ~/brain/knowledge/experts-clones)."
- options:
  - label: "I'll type it" — description: "Ask me for the path."
  - label: "Not set up yet" — description: "Skip for now. You can re-run /council:setup later when your directory is ready."

If "I'll type it": ask as a follow-up message "Path to experts-clones:". Expand `~` to full home path. Check if the directory exists with Glob. If not found, warn but continue — the user may create it later.

If "Not set up yet": inform the user that `/council:spawn` and `/council:assemble` won't work until this is configured. Stop here — do not write a config.

## Step 3 — Topics index

Check if `{experts_clones_path}/topics-index.yml` exists.

If yes: inform user it was found automatically.

If no: ask with AskUserQuestion:
- header: "Topics index"
- question: "topics-index.yml not found. Without it, /council:assemble <topic> won't work — but /council:spawn <slug> still will."
- options:
  - label: "Skip for now" — description: "Set topics_index_path to none. You can add it later."
  - label: "I'll provide a path" — description: "Ask me for the path."

## Step 4 — Agents output scope

Ask with AskUserQuestion:
- header: "Agent scope"
- question: "Default lifecycle for spawned/assembled agents? (You can always override with --ephemeral, --persist, or --user.)"
- options:
  - label: "Ephemeral" — description: "Session-first: files go to .claude/agents/ but are tracked in .claude/council/ephemeral-registry and removed with /council:dismiss. Best for reviews and experiments (avoids committing generated agents)."
  - label: "Project (persistent)" — description: "Save to .claude/agents/ in the project — survives dismiss; commit only the agents you want in git."
  - label: "User" — description: "~/.claude/agents/ — available in all projects; persistent."
  - label: "Ask each time" — description: "Prompt every spawn/assemble."

Map: Ephemeral → `ephemeral`, Project (persistent) → `project`, User → `user`, Ask each time → `ask`.

## Step 5 — Write config

Write `${CLAUDE_PLUGIN_ROOT}/config.yml`:

```yaml
# council config — generated by /council:setup
experts_clones_path: <expanded absolute path>
topics_index_path: <absolute path or "none">
# ephemeral | project | user | ask
agents_output_scope: ephemeral
```

Show the written config and confirm:

```
✓ Config saved.

/council:spawn <slug>        → spawn a single expert
/council:assemble <topic>    → assemble a team by topic
/council:setup               → re-run this wizard
```
